# Getting Started

1. Create a virtual environment in Python using `python -m venv venv`
3. Start up the virtual environment using `venv/Scripts/Activate`
4. Download the OpenVINO model to your local using `python prepare.py`
5. Start the generation using `python main.py`
